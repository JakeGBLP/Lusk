rootProject.name = 'Lusk'
include ':version_adapter'

file('skript').listFiles()?.each { dir ->
    if (dir.isDirectory()) {
        include ":skript:${dir.name}"
    }
}

file('nms').listFiles()?.each { dir ->
    if (dir.isDirectory()) {
        include ":nms:${dir.name}"
    }
}